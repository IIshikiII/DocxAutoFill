[Unit]
Description=Periodic renewal of the DocxAutoFill TLS certificate

[Timer]
# Short-lived certs (~6 days): check twice a day, renew when <3 days remain.
OnCalendar=*-*-* 03,15:00:00
RandomizedDelaySec=1h
Persistent=true

[Install]
WantedBy=timers.target
